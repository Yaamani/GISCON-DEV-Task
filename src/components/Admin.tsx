import { FC } from "react";
import { IUser, IUserAdminRole } from "../utils/Types";

type AdminPageProps = {
    // user: IUser<IUserAdminRole>;
};

const Admin: FC<AdminPageProps> = () => {
    
    return (
        <div>Admin</div>
    )
};

export default Admin;
